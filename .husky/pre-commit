# Check for env vars not in .env.example
"$(dirname "$0")/check-env-vars.sh"

# Run lint-staged to lint staged files and run related tests
pnpx lint-staged

# Run full test suite
pnpm run test:run

# Run typecheck on entire project
pnpm run typecheck

# Run build to ensure project builds successfully
pnpm run build